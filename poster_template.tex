\documentclass{proc}
\usepackage[utf8]{inputenc}
\usepackage{textgreek}
\usepackage{framed,enumitem}
\usepackage{fontspec}
\setmainfont{Calibri}
\usepackage{geometry}
\geometry{
 papersize={8.5in,11in},
 left=1in,
 right=1in,
 bottom=.5in,
 top=.44in,}
\begin{document}
\onecolumn
\begin{framed}
\begin{center}
  \vspace{-1em}
  \textbf{\VAR{session_name}}

  \textbf{\VAR{session_times} }
  \vspace{-1em}
\end{center}
\end{framed}
\begin{description}[leftmargin=4em,style=nextline]
%% for p in presentations
\vbox{%
    \item[\textbf{\VAR{session_type}\VAR{p.number}}]
    \textbf{\VAR{p.first_name} \VAR{p.last_name}}

    \underline{\VAR{p.author}}\textsuperscript{\VAR{p.author_ss}}\BLOCK{ for c in p.coauthors }, \VAR{c.name}\textsuperscript{\VAR{c.ss}}\BLOCK{ endfor }

    \emph{``\VAR{p.title}'' }

    %% for name in p.institutions
    \VAR{name}\textsuperscript{\VAR{loop.index}}

    %% endfor
    \vspace{1em}
    }
%% endfor
\end{description}
\end{document}
